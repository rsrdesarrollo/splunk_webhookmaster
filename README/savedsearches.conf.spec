
[httpalert]
httpalert.param.endpoint = <string> Endpoint. It's a required parameter.
httpalert.param.qs_params = <string> Querystring Params.
httpalert.param.custom_headers = <string> JSON Custom Headers object.
httpalert.param.credentials = <string> JSON Custom Headers object.
httpalert.param.payload = <string> Body.
httpalert.param.credential = <list> Credentials to use when calling webhook.
httpalert.param.method = <list> Method. It's a required parameter. It's default value is post.
httpalert.param.verify_ssl_certificate = <list> Should Requests verify the endpoint's certificate. It's a required parameter. It's default value is True.
